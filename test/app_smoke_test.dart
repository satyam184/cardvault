import 'package:flutter_test/flutter_test.dart';

import 'package:cardvault/main.dart';

void main() {
  test('CardVaultApp can be created', () {
    expect(const CardVaultApp(), isA<CardVaultApp>());
  });
}
